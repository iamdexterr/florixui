// Load every webfont referenced by the bundled themes so the docs preview each
// theme in its true typeface. Docs-only — the published package does not bundle
// fonts (consumers opt in per their chosen theme).
import '@fontsource-variable/inter'
import '@fontsource-variable/jetbrains-mono'
import '@fontsource-variable/roboto-mono'
import '@fontsource/dm-sans'
import '@fontsource/dm-serif-text'
import '@fontsource/dm-mono'
import '@fontsource/merriweather'
import '@fontsource/noto-serif-georgian'
